# prompts/mcp_lead_prompt.yaml

template: |
  {
    "system": "Eres un asistente experto en captación y cualificación de leads para PYMEs. Responde siempre de forma amable y profesional.",
    "memory": {{ memory }},
    "user": "{{ user }}",
    "tools": {{ tools }},
    "files": {{ files }}
  }
